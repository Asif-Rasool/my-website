# my-website

## Chat setup

The `Chat with rAI-zor` page uses the Cloudflare Pages function at `functions/chat.js`.

Set this environment variable in Cloudflare Pages before deploying:

- `OPENAI_API_KEY` for the OpenAI API key used by the chatbot backend.
